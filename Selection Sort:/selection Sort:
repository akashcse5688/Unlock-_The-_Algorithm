void selectionSort(int arr[], int Size) {
    for (int i = 0; i < n - 1; i++) {
        for (int j = i+1; j < n; j++)
            if (arr[j] < arr[i]) //find minimum value and swap with large value.
                swap(&arr[j], &arr[i]);
    }
}
